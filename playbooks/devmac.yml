- name: devmac
  hosts: local
  tasks:
    - import_playbook: general.yml
    - import_playbook: generalmac.yml
    - import_playbook: dev.yml
    
    - name: install nodejs
      ansible.builtin.command: brew install node@18 tmux neovim git

    - name: install npm distributed command-line tools
      npm:
        global: yes
        name: "{{ item }}"
      with_item:
        - typescript
